#include <bits/stdc++.h>
using namespace std;

int main() {
    // Fast I/O (Crucial for C++)
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;
    while (t--) {
        // Your logic for ONE test case goes here
        // Read inputs -> Solve -> Print Output (usually ending with \n)
    }
    return 0;
}